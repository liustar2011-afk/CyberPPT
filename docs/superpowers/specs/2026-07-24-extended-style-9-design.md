# CyberPPT 扩展风格9设计
## 目标

保留现有固定风格4“象牙白＋深蓝强调”及默认8种选择流程，新增可显式选择的扩展风格9“象牙白＋深蓝场景叙事”，迁移 `D:\CyberPPT-Stage2` 已验证的开放视觉语法与质感表达。

## 边界

- 默认风格目录仍为1—8，既有风格ID、名称、样张和项目行为不变。
- 风格9进入扩展风格目录，可通过 `style_id=9` 或其 slug 显式选择。
- 风格9沿用风格4色板，但允许低饱和实景图片、语义插画、低对比背景纹理、浅阴影、局部渐变、色带、空间轨迹和轻微立体层次。
- 开放表达不能制造业务关系、遮挡正文、改变来源事实或把条件性内容升级为确定结论。
- 当前项目已生成的风格4代表页保留，不被覆盖。

## 架构

样式库负责颜色、材质、线条、光影、图片色调和视觉语气。独立的开放视觉语法负责容器、连接、图文组合和层级边界；提示词组装器只渲染一次该语法。默认选择界面继续读取1—8，显式扩展选择可以读取风格9。

## 风格合同

- `id`: 9
- `slug`: `ivory_deep_blue_scene`
- `name`: `象牙白 + 深蓝场景叙事`
- 色板与风格4一致。
- 场景：方案型汇报、数字化建设、运行闭环、业务场景、具有实景或语义插画价值的页面。
- 密度：中高密度；主视觉可以扩大，但支撑结论和关键业务节点必须保留。

## 验收

- 风格库包含9个唯一ID，原1—8对象逐字段保持不变。
- 默认风格选择仍只返回1—8。
- 显式选择9能够返回新增合同并进入最终提示词。
- 最终提示词包含一次开放视觉语法，不包含风格4原有的固定矩阵、右侧栏、编号芯片等版式指令。
- 新增独立 `palette-09.png`，原 `palette-04.png` 不变。
