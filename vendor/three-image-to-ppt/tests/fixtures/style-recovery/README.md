# Style recovery fixtures

Tests generate registered FULL, BACKGROUND, and TEXT images in temporary directories with Pillow. No binary fixture is committed. The generated cases cover white text on navy, navy text on white, green highlighted text, and the low-confidence TEXT-only fallback.
