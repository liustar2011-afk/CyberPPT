# 方案架构师多结构竞争

本环节只在总编独立判断通过后执行，由方案架构师形成 `analysis/editorial/storyline-candidates.json`，不得读取或预设总编结构裁决。至少提出两套实质不同的结构，差异须体现在组织主线、章节序列、方案对象覆盖或内容权重，不能只改标题。

每套候选使用稳定 `candidate_id`，记录组织主线、完整章节、方案对象覆盖、内容权重、建议页数、压缩内容、适用条件、主要风险、推荐或淘汰理由与 Source ID；记录所有上游输入 SHA256。方案架构师只生成候选，不选择最终方案。
