"""Internal modules for the ImageGen handoff pipeline.

Import concrete submodules explicitly; the package itself intentionally has no
eager re-exports so lightweight rule modules remain free of delivery/CLI side
effects during first import.
"""
